/*global $:false, jQuery:false */

'use strict';

jQuery(function ($) {

    var helpers = {

        Elements: {
        },

        fixSocmedBar: function() {

        },

        init: function() {
            helpers.fixSocmedBar();
        }

    };

    // Initialize on DOM ready
    $(helpers.init);

});